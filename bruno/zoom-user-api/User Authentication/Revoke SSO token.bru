meta {
  name: Revoke SSO token
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/user/revoketoken
  body: formUrlEncoded
  auth: inherit
}

body:form-urlencoded {
  id: 
  email: 
}

example {
  name: Example revoke token response (Example revoke token request)
  
  request: {
    url: {{baseUrl}}/user/revoketoken
    method: POST
    mode: formUrlEncoded
    body:form-urlencoded: {
      id: 
      email: 
    }
  }
  
  response: {
    headers: {
      Content-Type: application/json
    }
  
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: json
      content: '''
        {
          "id": "unique_id",
          "email": "email_address",
          "updated_at": "2013-09-02T12:00:00.000Z"
        }
      '''
    }
  }
}
